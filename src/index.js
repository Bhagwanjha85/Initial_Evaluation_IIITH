import React from 'react';
import ReactDOM from 'react-dom/client';
import MFAAlignmentTool from './MFAAlignmentTool';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MFAAlignmentTool />
  </React.StrictMode>
);